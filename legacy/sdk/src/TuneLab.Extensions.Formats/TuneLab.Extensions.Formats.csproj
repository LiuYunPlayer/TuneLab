<Project Sdk="Microsoft.NET.Sdk">

  <!-- ════════════════════════════════════════════════════════════════════════
       冻结 · 禁改   Legacy SDK 源码快照
       老 format 插件契约（IImportFormat / IExportFormat + DataInfo），野外社区 format 插件链接它。
       · AssemblyVersion 钉死 1.0.0.0（野外绑定身份，不可动）；TFM 锁 net8（ABI 地板）。
       · 不进 TuneLab.sln，不跟随主仓 analyzer/props；取自 master 纯净 Legacy。
       破坏性演进走整代版本化 + Compat.Legacy 桥接。
       ════════════════════════════════════════════════════════════════════════ -->

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <AssemblyVersion>1.0.0.0</AssemblyVersion>
    <FileVersion>1.0.0.0</FileVersion>
    <InformationalVersion>1.0.0.0-frozen-legacy</InformationalVersion>
    <Version>1.0.0.0</Version>
    <Deterministic>true</Deterministic>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\TuneLab.Base\TuneLab.Base.csproj" />
  </ItemGroup>

</Project>
