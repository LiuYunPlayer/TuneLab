// TuneLab.SDK.Effect — effect 插件 SDK（ABI 地板 / net8 LTS）。
//
// 占位骨架：assembly 已就位、依赖（Primitives + SDK.Base）已连好，但 effect 插件接口的
// 完整形状（IEffectEngine 的 process 签名、能力拆分、dirty 事件传播…）尚未确定。
// 遵循"不提前建空壳 / 内核按需增长"的纪律，此处不预先冻结任何接口。
//
// 真实契约确定后填入本命名空间 TuneLab.SDK.Effect。
namespace TuneLab.SDK.Effect;
